---
en: |
  You're looking at it. Built lean, mostly static, no trackers. Will iterate as the studio grows.

  The site is plain HTML + CSS + a sprinkle of ES modules, bundled with Vite and deployed to GitHub Pages. No CMS, no database, no analytics. Devlog posts are markdown files in this repo — when I want to publish, I commit a `.md` and the site picks it up.

  Reasoning: the studio is one person right now. Every dependency is something I have to maintain. The simplest thing that could possibly work has the best chance of still working in two years when I'm deep in shipping Sand Coloring.

  ## Stack

  - Plain HTML / CSS / vanilla JS modules
  - Vite for the build + dev server
  - GitHub Actions for deploy
  - That's it.

  Total page weight: under 50KB gzipped before fonts.
vi: |
  Bạn đang nhìn nó đây. Build gọn, tĩnh, không tracker. Sẽ iterate khi studio lớn lên.

  Site là HTML + CSS + một chút ES modules, bundle bằng Vite và deploy lên GitHub Pages. Không CMS, không database, không analytics. Các bài devlog là file markdown trong repo này — khi muốn publish, mình commit một `.md` và site tự lấy.

  Lý do: studio hiện một người. Mỗi dependency là một thứ phải maintain. Cái đơn giản nhất có thể hoạt động sẽ có cơ hội cao nhất vẫn còn hoạt động hai năm nữa khi mình đang ngụp lặn ship Sand Coloring.

  ## Stack

  - HTML / CSS / vanilla JS modules
  - Vite cho build + dev server
  - GitHub Actions cho deploy
  - Hết.

  Tổng dung lượng trang: dưới 50KB gzipped trước khi tính font.
---
